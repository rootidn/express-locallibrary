extends layout 

block content
    h1 Genre List 

    ul 
        each genre in genre_list
            li 
                a(href=genre.url) #{genre.name}

        else 
            li there is no genre.
